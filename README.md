# local-text-scrapper
Python script that will walk through a directory you specify, copy the content from all the text-based files, and save it all into a single .txt file, preserving the directory structure for clarity.
